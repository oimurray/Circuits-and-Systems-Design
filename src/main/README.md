# Lab 1 circuits design TCD

## Probe wiring:

- Op-amp output → A0
- Shared ground (op-amp + signal source) → GND

## OLED SPI wiring:

- OLED D0 → D13
- OLED D1 → D11
- OLED CS → D10
- OLED DC → D9
- OLED RES → D8
- OLED VCC → 5V
- OLED GND → GND
